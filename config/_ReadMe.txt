Do not make any changes to the copy_... .json files.
These are here to define the desired layout - the website rendering works with these layouts.
The non-copy files can be changed - and are also automatically changed by the config-server.
copy files are meant as a backup.